# NASA API key (https://api.nasa.gov) used by the Storm Forecast page (DONKI/GST).
# Without it the app falls back to NASA's rate-limited DEMO_KEY.
# NOTE: VITE_ variables are bundled into the client build - only put values you're okay exposing publicly.
VITE_NASA_API_KEY=
